pub mod chat;
pub mod models;
pub mod schema;
// struct PropmtRepo<'repo> {
//     uri: &'repo str,
// }

// impl<'repo> PropmtRepo<'repo> {
//     fn new(uri: &'repo str) -> Self {
//         PropmtRepo { uri: uri }
//     }
//     fn build(self) -> String {

//     }
// }
